import { GuarenteedStopLossOrderModePipe } from './guarenteed-stop-loss-order-mode.pipe';

describe('GuarenteedStopLossOrderModePipe', () => {
  it('create an instance', () => {
    const pipe = new GuarenteedStopLossOrderModePipe();
    expect(pipe).toBeTruthy();
  });
});
