import { WeekOfMonthPipe } from './week-of-month.pipe';

describe('WeekOfMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new WeekOfMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
