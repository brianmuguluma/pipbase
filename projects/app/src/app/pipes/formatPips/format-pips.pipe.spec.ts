import { FormatPipsPipe } from './format-pips.pipe';

describe('FormatPipsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPipsPipe();
    expect(pipe).toBeTruthy();
  });
});
