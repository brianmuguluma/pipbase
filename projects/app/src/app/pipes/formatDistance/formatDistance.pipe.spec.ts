import { FormatRelativePipe } from './formatDistance.pipe';

describe('FormatDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRelativePipe();
    expect(pipe).toBeTruthy();
  });
});
