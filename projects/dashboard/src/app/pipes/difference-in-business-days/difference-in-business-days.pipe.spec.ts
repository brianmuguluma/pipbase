import { DifferenceInBusinessDaysPipe } from './difference-in-business-days.pipe';

describe('DifferenceInBusinessDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new DifferenceInBusinessDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
