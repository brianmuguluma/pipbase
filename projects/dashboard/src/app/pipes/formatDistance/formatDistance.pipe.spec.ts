import { FormatDistancePipe } from './formatDistance.pipe';

describe('FormatDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
