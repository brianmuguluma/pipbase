import { GetDaysInMonthPipe } from './get-days-in-month.pipe';

describe('GetDaysInMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDaysInMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
