import { StartOfMonthPipe } from './start-of-month.pipe';

describe('StartOfMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new StartOfMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
