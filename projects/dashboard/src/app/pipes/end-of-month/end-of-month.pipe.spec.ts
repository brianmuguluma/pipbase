import { EndOfMonthPipe } from './end-of-month.pipe';

describe('EndOfMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new EndOfMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
