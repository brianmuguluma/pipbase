import { RejectReasonPipe } from './reject-reason.pipe';

describe('RejectReasonPipe', () => {
  it('create an instance', () => {
    const pipe = new RejectReasonPipe();
    expect(pipe).toBeTruthy();
  });
});
