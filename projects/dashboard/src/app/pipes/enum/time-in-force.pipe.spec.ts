import { TimeInForcePipe } from './time-in-force.pipe';

describe('TimeInForcePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeInForcePipe();
    expect(pipe).toBeTruthy();
  });
});
